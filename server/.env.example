# AWS S3 Configuration Required

Please add the following variables to your `server/.env` file to enable image uploads and AI analysis:

```env
# AWS S3 Credentials
AWS_REGION=your-region (e.g., ap-northeast-2)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_S3_BUCKET=your-bucket-name
```

> [!IMPORTANT]
> Without these variables, the server will error out when attempting to upload new works or profiles.
